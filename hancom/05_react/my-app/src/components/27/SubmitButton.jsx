import Button from '@mui/material/Button'

const SubmitButton = () => {
    return (
        <Button variant="contained" onClick={() => alert('안녕하세요')}>
            누름
        </Button>

    )
}
export default SubmitButton